jobs:
  lint:
    executor: node/default
    steps:
      - checkout
      - run: npm ci
      - run: npm run lint
version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  lint:
    jobs:
      - lint
